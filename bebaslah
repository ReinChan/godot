extends Node2D
#variabel global
var nama_variabel=1

func _ready():
    #variabel lokal
    var variabel_lokal=10
    print(nama_variabel)
    
    #larangan dalam penulisan variabel
    
    # * Variabel tidak boleh diawali dengan angka.
    #var 1siswa
    #* Variabel tidak boleh mengandung spasi.
    #var nama siswa
    #* Variabel tidak boleh menggunakan kata yang digunakan dalam library Godot.
    #var print
    #* Variabel tidak boleh mengandung karakter husus.
    #var )siswa
    var siswa1
    var nama_siswa
    var printer
    var siswa
    pass
    
    
    
    #boolean
    var a =true
    #integer (bilangan bulat)
    var b=1
    #float (bilangan rill)
    var c=1.1
    #string 
    var d="kalimat"
    d=1
    
    
     #struktur
    #var nama_dictionary={kunci1:nilai1,kunci2:nilai2}
    var kamus={1:"nilai1","kunci2":2}
    # mengakses dictionary (mengambil)
    #print(kamus[1])
    # mengakses dictionary (merubah/ menambah)
    kamus[1]="dirubah"
    kamus["tambah"]="ditambah"
    #fungsi untuk mengambil panjang data
    #print(kamus.size())
    #fungsi untuk mengosongkan data
    #kamus.clear()
    #fungsi untuk menghapus data
    kamus.erase(1)
    print(kamus)
    
    
     #mendeklarasikan aray
    var a=[1,2,3,"hai"]
    #merubah nilai elemen array
    a[0]=10
    #menampilkan array
    #print(a[0])
    #fungsi untuk mengetahui panjang array
    #print(a.size())
    #menambahkan elemen array dibelakang
    a.push_back(60)
    #menambahkan elemen array didepan
    a.push_front(90)
    for i in a:
        print(i)
    
    
